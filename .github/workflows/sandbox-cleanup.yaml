name: Sandbox cleanup

on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run cleanup script
        run: node scripts/sandbox-cleanup.js
        env:
          HUUHO_UA: ${{ secrets.HUUHO_UA }}
          HUUHO_PASSWORD: ${{ secrets.HUUHO_PASSWORD }}
